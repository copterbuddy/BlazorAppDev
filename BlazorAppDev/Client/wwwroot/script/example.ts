function showAlert(message: string): void {
    alert(message + "from index.html");
}

function showCal(a: number, b: number): number {
    return a + b
}

function consoleLog(message: string) {
    console.log(message);
}